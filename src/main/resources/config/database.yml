---
  spring.config.activate.on-profile: local

  spring:
    # Datasource Configuration
    datasource:
      url: jdbc:mysql://127.0.0.1:33061/STUDY0911
      username: tester
      password: StrongPassword123!
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      show-sql: true
      properties:
        hibernate:
          ddl-auto: update
          format_sql: true
          default_batch_fetch_size: 20
---
